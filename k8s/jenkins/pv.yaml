apiVersion: v1
kind: PersistentVolume
metadata:
  name: jbpt-jenkins-volume
  labels:
    type: local
    app: jenkins-jbpt
spec:
  storageClassName: manual
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: /data/jenkins